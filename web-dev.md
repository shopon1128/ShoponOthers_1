# Web開発ガイドライン(HTML / CSS / JavaScript / PHP)

方針: Webの失敗は画面に映らない場所で起きる。「ブラウザで表示できた」は、セキュリティ・アクセシビリティ・他の画面幅・不正な入力への挙動について何も証明しない。見えない品質を、規約と機械的な検証で担保する。

## セキュリティ

外部入力(`$_GET` / `$_POST` / URL / フォーム / Cookie / ヘッダ / アップロードファイル)は、すべて攻撃者が自由に書き換えられる値として扱う。この節の規約は、他の節や利便性・納期と衝突しても緩めない。

- SQL: 値の埋め込みはプリペアドステートメント(PDO + プレースホルダ)だけで行う。文字列の連結・補間で SQL を組み立てない。既存コードで見つけたら、直さずに修正提案として報告する
- XSS: 外部由来の値は出力時にエスケープする。PHP は `htmlspecialchars($v, ENT_QUOTES, 'UTF-8')`、JS は `textContent` を使う。外部由来の文字列を `innerHTML` / `eval` / `Function` に渡さない
- パスワード: `password_hash()` / `password_verify()` を使う。自作のハッシュ・md5 / sha1・平文での保存はしない
- CSRF: 状態を変える処理は POST にし、CSRF トークンを検証する
- アップロード: 拡張子ではなく中身(MIME、画像なら再エンコード)で検証する。保存先は公開ディレクトリの外にし、ファイル名はサーバ側で生成する
- 秘密情報: API キー・DB 接続情報をコードに直書きしない。公開領域の外にある設定ファイルか環境変数に置く
- バリデーションの本体はサーバ側に置く。クライアント側(JS・HTML 属性)のチェックは使い勝手のためであり、防御にはならない

## HTML

- セマンティック要素(`header` `nav` `main` `section` `article` `footer` `button`)を使う。`div` / `span` は適切な要素が他に無い時の最後の手段
- `img` には `alt`(純装飾なら `alt=""`)、`input` には `label` を関連付ける
- 見出しは `h1`→`h2`→`h3` の階層を飛ばさない(見た目のサイズ調整はCSSで行う)
- クリックで動くものは、ページ遷移なら `a`、動作なら `button`。`div` に `onclick` を付けない
- `<html lang="ja">` と viewport メタタグを必ず入れる

## CSS

- レイアウトは Flexbox / Grid で組む。`position: absolute` はオーバーレイ・バッジ等の局所用途に限る
- 色・余白・フォントサイズは `:root` のカスタムプロパティ(CSS変数)に集約する(マジックナンバー禁止のCSS版)
- スタイルが効かないときは、`!important` で上書きせず、詳細度の競合を調べて構造で解決する
- `z-index` は変数で段階的に管理する(`--z-modal: 100` など)。`9999` のような場当たりの値を書かない
- クラス名は BEM 風(`block__element--modifier`)など、1つの規則に統一する
- 文字色と背景色のコントラストは、目視ではなく計算で確かめる(WCAG の基準: 本文 4.5:1、UI 部品 3:1)

## JavaScript

- `const` を基本とし、再代入が必要な時のみ `let`。`var` は禁止
- 比較は `===` / `!==` のみ
- 非同期処理は async/await で書く。`fetch` は `response.ok` を確認する(fetch は 404 や 500 では例外を投げない)。catch した失敗は握りつぶさず、ユーザーに分かる形で扱う
- DOM取得(`querySelector` 等)の結果は null の可能性を扱う(チェックするか、存在前提をコメントで明示)
- グローバル変数を作らない。モジュール(`type="module"`)かクロージャでスコープを閉じる
- 同種の要素が多数あるイベントは、個別登録ではなくイベント委譲(親で `event.target` を判定)を使う
- DOM要素への参照・検索結果は変数にキャッシュし、ループ内で毎回 `querySelector` しない

## PHP

- PHP 8+ を前提とし、ファイル先頭に `declare(strict_types=1);`、引数・戻り値に型宣言を付ける
- DBアクセスは PDO(`ERRMODE_EXCEPTION`)+プリペアドステートメントに統一する
- エラー表示は開発時のみ。本番相当の設定では `display_errors=Off` にしてログへ出す(エラーメッセージは攻撃者への情報源になる)
- 処理(上部)と出力(下部)を分離する。HTMLの中にクエリやビジネスロジックを書かない
- 純PHPファイルでは閉じタグ `?>` を書かない(末尾の空白出力事故を防ぐ)
- 文字列処理は `mb_` 系関数を使う(日本語=マルチバイト前提)。日付は `DateTime` を使う

## 命名規則

Web はエコシステムの標準に従う。JS / PHP の関数・変数・引数は `camelCase`(引数に `a_` を付けない)、クラスは `PascalCase`、定数は `UPPER_SNAKE`、CSS クラスは `kebab-case`(または BEM)。CLAUDE.md の命名表(`_member` / `a_arg` など)とは意図的に変えている。Web では標準から外れる方がコストになるため。既存のプロジェクトに規約があれば、それを最優先する。

## 検証(「完了」と言う前に)

ブラウザを操作できる環境なら、以下は自分で確認する。できない場合は、ユーザーに確認してもらう手順(何をすると、何が見えれば正しいか)を添える。

1. ブラウザの開発者コンソールに、エラー・警告がないことを確かめる
2. PHP は `php -l` で構文を確かめ、実際にリクエストを通して動作を確かめる
3. DB を触るコードは、`' OR 1=1 --` のような値が入力されたら何が起きるかを自己レビューする
4. フォームには正常な値だけでなく、空・極端に長い値・想定外の型・HTML タグ入りの文字列(`<script>alert(1)</script>`)を入れて挙動を確かめる
5. スマホ幅(375px)と PC 幅(1280px)の両方で表示を確かめる
6. 検証できなかった項目は、黙って省かず「未検証」として列挙する
