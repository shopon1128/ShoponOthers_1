# CLAUDE.md

> **テンプレートの使い方(コピー後にこのブロックごと削除する)**
> 1. 「プロジェクト情報」を埋める。分からない項目は空欄で残さず行ごと消す(空欄は Claude が推測で補ってしまうため)
> 2. 「基本原則」〜「命名規則(共通)」と「Git」はそのまま残す
> 3. 言語・ツール別のセクション(Unity / C++ / Python / SQL / Blender)は使うものだけ残す
> 4. Web を扱うなら `web-dev.md` もプロジェクトルートにコピーする。扱わないなら、`web-dev.md` に触れている2行(「作業の進め方」「命名規則(共通)」)を消す

常に日本語で応答する。

## プロジェクト情報

- 概要: (何を作っているかを1〜2行で)
- 技術スタックとバージョン: (例: Unity 6000.0 / URP、Python 3.12、PostgreSQL 16、Blender 4.2)
- ビルド: `(コマンド)`
- テスト: `(コマンド)`
- 実行: `(コマンド)`
- 手を触れない場所: (実データ・自動生成物・外部で管理しているファイルなど)
- 環境: macOS / VS Code

## 基本原則

判断に迷ったら、ここに立ち返る。

1. **記憶より現物** — パス・シンボル・API・設定キーは、grep・ファイル読取・公式ドキュメントで存在を確かめてから使う。確かめられないものは、もっともらしく補わずに「未確認」と書く。
2. **完了は検証の後** — 実行・テスト・ビルドで確かめてから「完了」と報告する。検証できなかった部分は「未検証」と明記し、どうすれば検証できるかを添える。
3. **前提を疑う** — 依頼に含まれる診断(「〜が原因」など)が疑わしければ、着手前に確かめ、違っていれば指摘する。指示どおりの修正が真因を隠す場合は、実装せずにその旨を報告する。
4. **根拠で答える** — 反論されたら、同意する前にも撤回する前にも根拠に戻って再確認する。指摘されただけで正しい実装を取り下げない。誤りと分かったら、はっきり認めて直す。
5. **スコープを守る** — 頼まれたことを、頼まれた分だけ行う。関係のない改善は実施せず、一行で提案する。データ破壊・セキュリティなどの重大な問題を見つけたら、勝手に直さずすぐ報告する。
6. **確認は必要なときに、まとめて** — 答えによって作業内容が変わる曖昧さは、着手前に質問する。質問は一度にまとめ、選択肢(①②③)と推奨案を添えて一往復で決められる形にする。結果を左右しない細部は妥当な既定値で進め、何を仮定したかを報告に書く。

## 作業の進め方

- 3ファイル以上に及ぶ変更は、着手前に方針を数行で示して合意を取る
- 編集前に、対象ファイルと主要な呼び出し元を読む。修正後は、その修正が壊しうる箇所を1つ以上確認する
- エラーは真因を直す。try/catch や null チェックで症状を消すのは修正ではない
- ファイルの作成・削除・移動など副作用のある操作は、終了コードだけで成功と判断せず、`ls` などで結果を確かめてから次に進む
- 検証はできる限り機械的な手段(grep・スクリプト・テスト実行)で行い、目視だけで済ませない
- 新規ファイルの作成より、既存ファイルの編集を優先する
- ファイルの変更は Edit / Write ツール、閲覧は Read ツールで行う。`sed -i`・`perl -i`・ヒアドキュメントでの上書きや、`cat`・`sed -n` での閲覧はしない(変更を差分として確認できるようにするため)
- プロジェクトルートに `LESSONS.md` があれば、作業前に読む。解決に手間取った問題や予想外の挙動に出会ったら、そのファイルの運用ルールに従って追記する
- プロジェクトルートに `web-dev.md` があれば、HTML / CSS / JavaScript / PHP を触る前に読む。Web固有の規約(セキュリティ・アクセシビリティ・命名)はそちらが優先される

## 過去のプロジェクトで繰り返した失敗から

調査・デバッグ
- 同じ症状への修正が3回続いても直らなければ、手を止める。症状が出ている層ではなく、別の層(データ・生成・設定など)に原因がないかを疑う
- 事前チェックと実際の処理が、同じ対象・同じ基準点を使っているかを確かめる(例: 射線チェックは現在位置、発射は予測位置、というずれ)
- 無関係に見える症状がいくつも同時に出たら、まず例外ログを見る。初期化の途中で起きた例外は、その後の処理をまとめて飛ばす
- 絵や部品が何を表しているかを、形や構成から推測しない。意味が動作に関わるなら作者に確認する

測定・データ分析
- 少ない試行で出た最良の結果を実力とみなさない。「選ぶ」試行と「評価する」試行は分け、重要な結論は再現させる
- 2つの条件を比べるときは、順番に測らず交互に測る(A/B)。順番に測ると、時間とともに変わる要因が混ざる
- 集計結果が既知の事実と食い違ったら、計算より先に列の意味(どちらの立場の記録か)を疑う

テスト・環境
- テストは一時ディレクトリの中だけで完結させる。実データがありうるパスを削除・上書きするテストは書かない
- OS 付属のコマンドに依存する処理は、コマンドがない環境の代替処理だけをテストして済ませない。実際にコマンドがある環境で動くまで「完了」と言わない

## 事前に許可を取る操作

次の操作は、実行前に内容と影響を説明し、許可を得てから行う。許可はそのタスク限りとし、以降のタスクに流用しない。

- ファイル・ディレクトリの削除
- 既存の動作が変わる変更(意図した仕様変更も含む)
- Git の破壊的操作(`git push --force`、`git reset --hard`、`git checkout .` など)
- 言語・ツール別セクションで指定した操作(SQL の `DROP` など、Unity の YAML 直接編集)

## 報告

- 結論を先に、簡潔に書く
- 「確認済み」「推測」「未検証」を書き分け、すべてを同じ断定調で書かない
- 提案には、なぜそうするのかを一言添える

## コーディング全般

- 可読性と拡張性を重視する。種類が増えると見込まれる分岐は、ポリモーフィズム(インターフェース・継承・ストラテジーなど)に置き換えて拡張しやすくする。ただし、現時点で使い道のない抽象化・設定項目・汎用化は作らない
- データとロジックを分け、データ(設定値・テーブル・パラメータ)をロジックに手を入れずに差し替えられるようにする
- コメントには「なぜそうするのか」を書く。コードを読めば分かる「何をしているか」の説明は書かない
- マジックナンバーは使わず、名前を付けた定数にする
- 既存コードのスタイルに合わせる。このファイルの規約が既存コードや環境の現実(ライブラリの不在・バージョン差など)と食い違う場合は現実に合わせ、逸脱として報告する

## 命名規則(共通)

| 種別 | ルール | 例 |
|---|---|---|
| メンバ変数 | 先頭に `_` | `_plSpeed`, `_btnMenu` |
| 定数・const にできない定数的フィールド | 大文字 + `_` | `DEF_POWER`, `MAX_HP` |
| メソッド引数 | 先頭に `a_` | `Damage(int a_damage)` |
| ローカル変数 | camelCase | `moveDir`, `hitCount` |
| メソッド名 | PascalCase | `ApplyDamage()`, `InitPlayer()` |

- 名前は分かりやすく、長すぎないようにする
- 略語は一般的なものだけ使う(pl=Player, btn=Button, def=Default など)
- 言語・ツール別セクションに別の規則がある場合は、そちらを優先する
- この表は C# / C++ / Python 向け。Web(HTML / CSS / JS / PHP)には適用せず、`web-dev.md` の命名規則に従う

## Unity

方針: Claude は Unity Editor の画面や Play 中の挙動を確認できない。コードの正しさは読んで詰め、動作確認はユーザーに具体的な手順で依頼する。

- Unity 6 を使い、入力は Input System で扱う(旧 `UnityEngine.Input` は使わない)
- 命名は共通の命名規則に従う(Microsoft の C# 規約と異なる部分は、共通の命名規則を優先する)
- 定数として扱う `[SerializeField]` フィールドは、const でなくても `DEF_POWER` 形式で命名する
- `[Header("セクション名")]` と `[Tooltip("その変数の説明")]` で Inspector を整理する
- 中身のない `Update()` / `Start()` などのイベント関数は削除する(空でも Unity から呼び出されるため)
- `GetComponent` の結果はキャッシュして使い回す
- UniTask が導入済みなら、コルーチンより async/await(UniTask)を優先する
- Inspector の割り当て漏れは、エラーを出して止まるようにする。`if (obj != null)` などで黙ってスキップすると、漏れに気づけなくなる
- シーン・プレハブなどの YAML の直接編集は、タスクごとに事前に許可を取ってから、そのタスクの範囲内でのみ行う(Editor で開いているシーンは Editor 側の状態が優先され、外部からの編集と上書きし合うため)
- コード変更後は「Editor でこう確認してほしい」という手順(何をすると、何が見えれば正しいか)を必ず添える。未確認のまま「動きます」と書かない

## C++

方針: C++ では「クラッシュせずに動いた」は正しさの証明にならない(未定義動作は黙って通る)。正しい書き方をデフォルトにし、検証は機械に任せる。

- C++17 以上を前提とする(「プロジェクト情報」に指定があればそれに従う)
- 所有権を型で表す。所有するなら `std::unique_ptr`(共有所有が本当に必要な場合だけ `std::shared_ptr`)、所有しない参照は生ポインタ/参照。`new` / `delete` を直接書かない(RAII を徹底する)
- Rule of Zero を基本とする。デストラクタ・コピー・ムーブのどれか1つでも定義するなら、5つすべてを明示する(Rule of Five)
- 生配列・`char*` ではなく `std::array` / `std::vector` / `std::string` / `std::string_view` を使う
- 変数は宣言時に初期化し、`{}` 初期化を優先する
- `const` を付けられる場所(変数・メンバ関数・参照引数)にはすべて付ける
- キャストは `static_cast` などの C++ キャストだけを使う。C スタイルキャストは使わない
- マクロで定数・関数を作らず、`constexpr` / `inline` を使う
- 寿命に注意する。ローカル変数への参照を返さない、参照キャプチャしたラムダをスコープ外に保存しない、コンテナの変更中にイテレータ・参照を使い続けない
- ビルドは `-Wall -Wextra -Werror` を前提とし、警告ゼロを保つ
- 実行検証はできる限り AddressSanitizer / UBSanitizer 付き(`-fsanitize=address,undefined`)で行う。通常の実行が通っただけなら「未検証」として報告する
- エラー処理の方針(例外 or 戻り値)は既存コードに合わせ、1つのコードベースで混在させない
- 迷ったら C++ Core Guidelines に従う

## Python / 機械学習

- Python 3.11 以上 / 型ヒントを付ける / docstring は日本語
- フレームワークは、特に指定がなければ PyTorch
- フォーマッタは black / isort に準拠する
- データ処理には pandas・numpy を使う
- 学習スクリプトの設定は argparse または Hydra で外部化する
- シード値を固定する(再現性のため)
- スクリプトを書いたら、できる限り小さなデータ・少ないステップ数で実際に実行してから納品する
- データの読み込み・前処理では、不正な行や欠損を黙って捨てず、件数と内容を報告する

## SQL

方針: SQL は1文で全データを壊せる。書き込み系は影響範囲を数字で確かめてから実行し、本番データには Claude 単独で触らない。

- 方言(PostgreSQL / MySQL / SQLite など)とバージョンは「プロジェクト情報」に書く。書かれていなければ着手前に確認する。方言に依存する構文を使うときは、その旨をコメントに残す
- テーブル名・列名は推測で書かない。スキーマ定義(DDL・マイグレーション・`\d` など)で確かめてから使う
- `UPDATE` / `DELETE` には必ず `WHERE` を付ける。実行前に同じ条件の `SELECT COUNT(*)` で影響行数を確かめ、その数を報告してから実行する
- `DROP` / `TRUNCATE` / `ALTER` / `WHERE` のない更新は、実行前に説明して許可を得る
- 本番 DB に直接クエリを発行しない。検証は開発用 DB・テストデータで行う
- 複数の文にまたがるデータ変更はトランザクションで囲み、失敗時に戻せるようにする
- アプリケーションコードの SQL はプレースホルダ(パラメータ化クエリ)で組み立てる。文字列結合で SQL を組み立てない
- `SELECT *` は避け、必要な列を明示する
- マイグレーション機構がある場合、スキーマ変更はマイグレーションファイルとして残し、直接 `ALTER` しない
- 性能の指摘・修正は `EXPLAIN` の結果を根拠にする。「インデックスを貼れば速くなるはず」と推測で書かない
- 予約語は大文字、識別子は snake_case(既存スキーマに規約があればそれに従う)

## Blender

方針: Claude は Blender の画面(ビューポート・レンダリング結果)を確認できない。スクリプトが実行できるかは機械的に検証し、見た目の正しさはユーザーに確認してもらう。

- Blender のバージョンは「プロジェクト情報」に書く。`bpy` の API はバージョンごとに変わるため、記憶にある関数名・プロパティ名は公式 API リファレンスで確かめてから使う
- `bpy.ops` はコンテキストに依存し、スクリプトからの実行で失敗しやすい。データ API(`bpy.data`、`obj.location = ...` など)で書けるものに `bpy.ops` は使わない
- `bpy.ops` を使う場合は、必要なコンテキスト(アクティブオブジェクト・選択状態・モード)を明示的に設定してから呼ぶ
- Edit モード中は `obj.data` への直接編集が反映されない。メッシュ編集は `bmesh` を使うか、Object モードに戻してから行う
- 一時的に作ったオブジェクト・メッシュ・マテリアルは `bpy.data.*.remove()` で削除し、孤立データを残さない
- 既存の .blend を書き換えるスクリプトは、実行前に別名保存(バックアップ)を促す。取り消せない操作(削除・モディファイア適用・結合)では特に
- 動作確認はできる限りバックグラウンド実行(`blender -b file.blend --python script.py`)で行い、エラーなく完走することを確かめてから納品する。GUI に依存して実行できない場合は「未検証」と明記する
- コード変更後は「Blender でこう確認してほしい」という手順(どのオブジェクトをどう見るか・期待する見た目)を必ず添える
- アドオンでは `register()` / `unregister()` を対にし、登録したクラス・ハンドラ・キーマップを `unregister()` ですべて解除する
- 座標系は Z-up・右手系・メートル単位を前提とする。他ツール(Unity は Y-up・左手系)との入出力では、軸と単位の変換を明示する
- Python スクリプトの命名は PEP 8 / `bpy` の慣習(snake_case)に従う(共通の命名規則より優先)

## Git

- コミットメッセージは日本語 + プレフィックス(`feat:`, `fix:`, `refactor:`, `docs:`)
- 1コミット1目的
- ビルドとテストが通る状態でのみコミットする
- 破壊的操作は「事前に許可を取る操作」に従う
