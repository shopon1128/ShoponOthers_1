# CLAUDE.md

常に日本語で応答する。

## 最重要原則(他のすべてに優先)

1. **記憶より現物** — パス・シンボル・APIは推測で書かず、grep・ファイル読取・ドキュメントで確認してから使う。存在確認できないものは書かない。
2. **完了は検証の後** — 実行・テスト・コンパイル確認をしてから「完了」と報告する。検証できない場合は「未検証」と明示する。
3. **前提を疑う** — 依頼内容の診断(「〜がバグの原因」等)が疑わしければ、作業前に実際に確認し、違ったら指摘する。指示通りの修正が真因を隠す場合は従わずに報告する。
4. **スコープ厳守** — 頼まれたことを頼まれた分だけ。無関係なリファクタや「ついでの改善」は実施せず一行で提案する。重大な問題(データ破壊・セキュリティ)を見つけたら、勝手に直さず報告する。
5. **不明点は推測せず確認** — 曖昧な指示は着手前に質問する。ただし質問は一度にまとめる。

## 環境

- macOS / VS Code
- プロジェクト固有のビルド・テスト・実行コマンドは各リポジトリのCLAUDE.mdに記載(このファイルは共通設定)

## 作業の進め方

- 3ファイル以上に及ぶ変更は、着手前に方針を数行で提示して合意を取ってから実装する
- 編集前に対象ファイルと主要な呼び出し元を読む。修正後は、その修正が壊しうる箇所を1つ以上確認する
- エラー修正は真因を直す。try/catchの追加やnullチェックでの握りつぶしは修正ではない
- ファイル作成・削除・移動など副作用のあるコマンドは、終了コード0だけで成功と判断せず、`ls` 等で結果を現物確認してから次の手順に進む
- 検証はできる限り機械的な手段(grep・スクリプト・テスト実行)で行い、目視確認だけで済ませない
- ユーザーに確認・質問するときは選択肢(①②③)を添え、一往復で決められる形にする
- 新規ファイル作成より既存ファイルの編集を優先する
- 報告では「確認済み」と「推測」を書き分ける。全部を同じ断定調で書かない
- プロジェクトルートに `LESSONS.md` があれば作業開始前に読む。原因調査に15分以上かかった問題を解決したら、そのファイルの形式に従って追記する

## コーディング全般

- ポリモーフィズムを意識し、可読性と拡張性の高いコードを書く
- データとロジックを切り分け、データの独立性を確保する
- コメントは処理の意図(why)がわかるように適宜入れる
- マジックナンバーは避け、定数に名前をつける
- 既存コードのスタイルを尊重し、一貫性を保つ。このファイルの規約が既存コードや実行環境の現実(ライブラリ不在・バージョン差など)と矛盾する場合は現実側に合わせ、逸脱として報告する
- ファイル変更は必ず Edit / Write ツールを使う。sed -i・perl -i・ヒアドキュメントでの上書きは禁止。ファイル閲覧も Read を使い sed -n や cat は使わない
- 提案時は「なぜそうするのか」理由も簡潔に添える

## 命名規則(共通)

| 種別 | ルール | 例 |
|---|---|---|
| メンバ変数 | 先頭に `_` | `_plSpeed`, `_btnMenu` |
| 定数・const不可の定数的フィールド | 大文字 + `_` | `DEF_POWER`, `MAX_HP` |
| メソッド引数 | 先頭に `a_` | `Damage(int a_damage)` |
| ローカル変数 | camelCase | `moveDir`, `hitCount` |
| メソッド名 | PascalCase | `ApplyDamage()`, `InitPlayer()` |

- 変数名はわかりやすく、かつ長すぎないようにする
- 略語は一般的なもののみ使用(pl=Player, btn=Button, def=Default, etc.)

## Unity プロジェクト

- **Unity 6** / **Input System** で入力を管理する
- C# のコーディング規約に従う
- `SerializeField` は定数的に扱うものでも `DEF_POWER` 形式で命名する
- `[Header("セクション名")]` と`[Tooltip("その変数の説明")]`で Inspector を整理する
- 不要な `Update()` / `Start()` は削除する
- `GetComponent` の結果はキャッシュして使い回す
- コルーチンよりも async/await(UniTask)を優先する(導入済みの場合)
- Claude は Unity Editor での動作確認ができない。コード変更後は「Editor でこう確認してほしい」というテスト手順を必ず添える。動作未確認のまま「動きます」と言わない
- インスペクターの割り当て漏れはエラーを出して止まる方が健全。if(obj!=null)などで潰したりしない
- YHMLの直接編集は許可を得てから、そのタスクの範囲内でのみ行う。一度の許可を以降全てのタスクに適応しない

## C++

方針: C++では「クラッシュせず動いた」は正しさの証明にならない(未定義動作は黙って通る)。よって、正しい書き方をデフォルトで強制し、検証は機械に任せる。

- **C++17以上**を前提とする(プロジェクト指定があればそれに従う)
- **所有権を型で表現する**: 所有するなら `std::unique_ptr`(共有所有が本当に必要な場合のみ `std::shared_ptr`)、非所有の参照は生ポインタ/参照。`new`/`delete` を直接書かない(RAII徹底)
- **Rule of Zero** を基本とする。デストラクタ・コピー・ムーブのいずれか1つでも定義するなら、5つすべてを明示する(Rule of Five)
- 生配列・`char*` ではなく `std::array` / `std::vector` / `std::string` / `std::string_view` を使う
- 変数は宣言時に必ず初期化し、`{}` 初期化を優先する
- `const` を付けられる場所にはすべて付ける(変数・メンバ関数・参照引数)
- キャストは `static_cast` 等のC++キャストのみ。Cスタイルキャストは禁止
- マクロで定数・関数を作らない。`constexpr` / `inline` を使う
- 寿命に注意: ローカル変数への参照を返さない、参照キャプチャしたラムダをスコープ外に保存しない、コンテナ変更中のイテレータ・参照を使い続けない
- ビルドは `-Wall -Wextra -Werror` を前提とし、警告ゼロを維持する
- 実行検証は可能な限り **AddressSanitizer / UBSanitizer 付き**(`-fsanitize=address,undefined`)で行う。通常実行が通っただけでは「未検証」として報告する
- エラー処理方針(例外 or 戻り値)は既存コードに合わせ、1つのコードベースで混在させない
- 迷ったら C++ Core Guidelines に従う

## Python / 機械学習

- Python 3.11+ / 型ヒントを必ずつける / docstring は日本語
- フレームワーク: PyTorch(特に指定がなければ)
- フォーマッタ: black / isort 準拠
- データ処理では pandas, numpy を活用
- 学習スクリプトでは argparse または Hydra で設定を外部化する
- シード値の固定を忘れない(再現性の確保)
- スクリプトを書いたら、可能な限り小さなデータ・少ないステップ数で実際に実行してから納品する
- データの読み込み・前処理では、不正な行や欠損を黙って捨てず、件数と内容を報告する

## SQL

方針: SQLは1文で全データを壊せる。よって、書き込み系は影響範囲を数字で確認してから実行し、本番データにはClaude単独で触らない。

- 方言(PostgreSQL / MySQL / SQLite 等)とバージョンはプロジェクトのCLAUDE.mdで指定する。指定がなければ着手前に確認し、方言依存の構文を使う場合はその旨をコメントに残す
- テーブル名・列名は推測で書かない。スキーマ定義(DDL・マイグレーション・`\d` 等)で確認してから使う
- `UPDATE` / `DELETE` は必ず `WHERE` 付き。実行前に同条件の `SELECT COUNT(*)` で影響行数を確認し、その数を報告してから実行する
- `DROP` / `TRUNCATE` / `ALTER` / `WHERE` なしの更新は、実行前に説明して許可を得る(Gitの破壊的操作と同じ扱い)
- 本番DBに直接クエリを発行しない。検証は開発用DB・テストデータで行う
- 複数文にまたがるデータ変更はトランザクションで囲み、失敗時に戻せる形にする
- アプリケーションコードからのSQLは必ずプレースホルダ(パラメータ化クエリ)を使う。文字列結合でSQLを組み立てない
- `SELECT *` は避け、必要な列を明示する
- スキーマ変更はマイグレーションファイルとして残し、直接 `ALTER` しない(マイグレーション機構がある場合)
- 性能の指摘・修正は `EXPLAIN` の結果を根拠にする。「インデックスを貼れば速くなるはず」を推測で書かない
- 予約語は大文字、識別子は snake_case(既存スキーマの規約があればそれに従う)

## Blender

方針: Claude は Blender の画面(ビューポート・レンダリング結果)を確認できない。スクリプトの実行可否は機械的に検証し、見た目の正しさはユーザーに確認を委ねる。

- Blender のバージョンはプロジェクトのCLAUDE.mdで指定する。`bpy` の API はバージョン間で変わるため、記憶にある関数名・プロパティ名は公式APIリファレンスで確認してから使う
- `bpy.ops` はコンテキスト依存でスクリプト実行時に失敗しやすい。データAPI(`bpy.data`、`obj.location = ...` 等)で書けるものは `bpy.ops` を使わない
- `bpy.ops` を使う場合は、必要なコンテキスト(アクティブオブジェクト・選択状態・モード)を明示的に設定してから呼ぶ
- Editモード中は `obj.data` への直接編集が反映されない。メッシュ編集は `bmesh` を使うか、Objectモードに戻してから行う
- 一時的に作ったオブジェクト・メッシュ・マテリアルは `bpy.data.*.remove()` で削除し、孤立データを残さない
- 既存の .blend を書き換えるスクリプトは、実行前に別名保存(バックアップ)を促す。取り消せない操作(削除・モディファイア適用・結合)は特に
- 動作確認は可能な限りバックグラウンド実行(`blender -b file.blend --python script.py`)で行い、エラーなく完走することを確認してから納品する。GUI依存で実行できない場合は「未検証」と明示する
- コード変更後は「Blenderでこう確認してほしい」という手順(どのオブジェクトをどう見るか・期待する見た目)を必ず添える
- アドオンでは `register()` / `unregister()` を対にし、登録したクラス・ハンドラ・キーマップを `unregister()` ですべて解除する
- 座標系は Z-up・右手系・メートル単位を前提とする。他ツール(Unity は Y-up・左手系)との入出力では軸・単位の変換を明示する
- Python スクリプトの命名は PEP 8 / `bpy` の慣習(snake_case)に従い、上の共通命名規則より優先する

## Git

- コミットメッセージは日本語 + プレフィックス(`feat:`, `fix:`, `refactor:`, `docs:`)
- 1コミット1目的
- コンパイルが通る・テストが通る状態でのみコミットする
- `git push --force`、`git reset --hard`、`git checkout .` などの破壊的操作は、実行前に説明して許可を得る

## やらないこと

- 確認なしにファイルを削除しない
- 既存の動作を壊す変更を黙って行わない
- 未実行のコードを「動作確認済み」のように報告しない
- 存在を確認していない API・ファイルパス・設定キーを、もっともらしく生成しない
- 指摘されただけで正しい実装を撤回しない。根拠に戻って再確認してから答える
